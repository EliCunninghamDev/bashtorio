{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/inject-bundle-size.mjs",
    "preview": "astro preview",
    "pages-deploy": "rm -f dist/v86/*-state.bin dist/v86/*-state.bin.br && node scripts/upload-state-r2.mjs && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "bashtorio-core": "workspace:*"
  },
  "devDependencies": {
    "@fontsource/jetbrains-mono": "^5.2.8",
    "wrangler": "^4.63.0"
  }
}
