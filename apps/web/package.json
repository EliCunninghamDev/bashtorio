{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/inject-bundle-size.mjs",
    "preview": "astro preview",
    "deploy": "rm -f dist/v86/alpine-state.bin && wrangler pages deploy"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "bashtorio-core": "workspace:*"
  },
  "devDependencies": {
    "@fontsource/jetbrains-mono": "^5.2.8",
    "wrangler": "^4.63.0"
  }
}
