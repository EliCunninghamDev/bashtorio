{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-manifest.mjs && astro build",
    "preview": "astro preview",
    "pages-deploy": "node scripts/upload-state-r2.mjs && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "bashtorio-core": "workspace:*"
  },
  "devDependencies": {
    "@fontsource/jetbrains-mono": "^5.2.8",
    "wrangler": "^4.63.0"
  }
}
