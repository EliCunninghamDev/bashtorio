# Full URL to pre-booted VM state on R2 (leave empty for local /v86/$PUBLIC_STATE_FILE)
# Use a .br URL to serve brotli-compressed state (decompressed client-side)
PUBLIC_STATE_URL=https://bigassets.bashtorio.xyz/alpine-state.bin.br
# Filename of the VM state snapshot inside /v86/ (default: alpine-state.bin)
PUBLIC_STATE_FILE=alpine-state.bin
