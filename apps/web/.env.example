# Remote origin for VM state file (leave empty for local /v86/)
PUBLIC_STATE_BASE_PATH=https://bigassets.bashtorio.xyz
# Filename of the VM state snapshot (default: alpine-state.bin)
PUBLIC_STATE_FILENAME=alpine-state.bin

# Cloudflare CDN cache purge after R2 upload (optional)
CF_ZONE_ID=
CF_PURGE_TOKEN=
